工作上遇到了要把torch模型转换成onnx再转换成tensorflow的需求，弄了半天将近4小时才搞明白。

在不懂神经网络之前，需要先知道
onnx是开放神经网络文件格式，用于交换，不过根据知识来说，似乎pytorch和tensorflow是对头，这种东西肯定不是官方的

onnx文件的格式为.onnx,tensorflow文件的格式为.pb

torch转到onnx只需要一个函数，torch.onnx.export(),参数主要需要一个model和一个example_input
model需要load_stat_dict，载入所有参数,然后需要创建一个样例输入
输入一般是Tensor的，注意好size就ok，随便生成个输入都可以


有onnx后可以用onnxruntime来验证模型


再转到tensorflow的时候会很慢，光转换过程大概花了5分钟？
不过可能是因为我转的是DNN，层数比较多，我看tensorflow的转换是以graph，里面会说明每一层的输入输出？
然后转出来的.pb文件直接交差了，我没有仔细验证，因为tensorflow的喂数据有点奇怪，需要一个session才能运行，不像torch直接喂就行


